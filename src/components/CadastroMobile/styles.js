import styled from "styled-components";

export const Wrapper = styled.div`
  width: 100vw;
  height: 100vh;
  position: absolute;
  top: 0;
  left: 0;
  background-color: rgba(var(--home_bg--grey), 0.7);
  z-index: 10;
  display: none;
  justify-content: center;
  align-items: center;
  transition: 1s;
`;
